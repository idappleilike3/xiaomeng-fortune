{
  "name": "fortune-line-web",
  "version": "0.1.0",
  "private": true,
  "description": "LINE official account webhook and premium fortune web MVP.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js",
    "rich-menu:image": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/generate-rich-menu.ps1",
    "rich-menu:setup": "node scripts/setup-line-rich-menu.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
